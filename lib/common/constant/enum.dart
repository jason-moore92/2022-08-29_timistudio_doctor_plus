enum ConsultType { appointment, voice, message, liveChat, video, account, bank }

enum StatusType {
  inProgress,
  completed,
  accepted,
  unconfirmed,
  iCancelled,
  patientCancelled,
  declined,
}
